
<template name="order-count">
  <button wx:if="{{count === 0}}" size="mini" plain>
    {{count}}
  </button>

  <button wx:if="{{count !== 0}}" size="mini" type="primary">
    {{count}}
  </button>
</template>